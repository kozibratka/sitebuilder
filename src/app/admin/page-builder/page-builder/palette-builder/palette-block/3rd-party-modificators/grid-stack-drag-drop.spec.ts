import { GridStackDragDrop } from './grid-stack-drag-drop';

describe('GridStackDragDrop', () => {
  it('should create an instance', () => {
    expect(new GridStackDragDrop()).toBeTruthy();
  });
});
