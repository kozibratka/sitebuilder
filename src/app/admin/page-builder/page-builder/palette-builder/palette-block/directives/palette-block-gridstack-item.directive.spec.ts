import { PaletteBlockGridstackItemDirective } from './palette-block-gridstack-item.directive';

describe('PaletteBlockGridstackItemDirective', () => {
  it('should create an instance', () => {
    const directive = new PaletteBlockGridstackItemDirective();
    expect(directive).toBeTruthy();
  });
});
