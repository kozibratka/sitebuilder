import { QuickMenuMessenger } from './quick-menu-messenger';

describe('QuickMenuMessenger', () => {
  it('should create an instance', () => {
    expect(new QuickMenuMessenger()).toBeTruthy();
  });
});
