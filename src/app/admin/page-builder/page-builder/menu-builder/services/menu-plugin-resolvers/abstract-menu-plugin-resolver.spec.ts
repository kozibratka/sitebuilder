import { AbstractMenuPluginResolver } from './abstract-menu-plugin-resolver';

describe('AbstractMenuPluginResolver', () => {
  it('should create an instance', () => {
    expect(new AbstractMenuPluginResolver()).toBeTruthy();
  });
});
