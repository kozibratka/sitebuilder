import { BaseSettingsAbstract } from './base-settings-abstract';

describe('BaseSettings', () => {
  it('should create an instance', () => {
    expect(new BaseSettingsAbstract()).toBeTruthy();
  });
});
