import { AbstractPluginComponent } from './abstract-plugin-component';

describe('AbstractPluginComponent', () => {
  it('should create an instance', () => {
    expect(new AbstractPluginComponent()).toBeTruthy();
  });
});
