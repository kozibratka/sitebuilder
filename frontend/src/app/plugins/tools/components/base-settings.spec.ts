import { BaseAdminAbstractComponent } from './base-admin-abstract.component';

describe('BaseSettings', () => {
  it('should create an instance', () => {
    expect(new BaseAdminAbstractComponent()).toBeTruthy();
  });
});
