import { MenuPluginResolverDirective } from './menu-plugin-resolver.directive';

describe('MenuPluginResolverDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuPluginResolverDirective();
    expect(directive).toBeTruthy();
  });
});
