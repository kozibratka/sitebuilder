import { ProgresBarDirective } from './progres-bar.directive';

describe('ProgresBarDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgresBarDirective();
    expect(directive).toBeTruthy();
  });
});
