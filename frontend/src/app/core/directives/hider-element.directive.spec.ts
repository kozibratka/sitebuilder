import { HiderElementDirective } from './hider-element.directive';

describe('HiderElementDirective', () => {
  it('should create an instance', () => {
    const directive = new HiderElementDirective();
    expect(directive).toBeTruthy();
  });
});
