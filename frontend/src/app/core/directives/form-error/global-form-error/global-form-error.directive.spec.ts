import { GlobalFormErrorDirective } from './global-form-error.directive';

describe('FormGroupErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new GlobalFormErrorDirective();
    expect(directive).toBeTruthy();
  });
});
