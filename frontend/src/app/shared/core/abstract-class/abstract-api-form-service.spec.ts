import { AbstractApiFormService } from './abstract-api-form-service';

describe('AbstractApiForm', () => {
  it('should create an instance', () => {
    expect(new AbstractApiFormService()).toBeTruthy();
  });
});
